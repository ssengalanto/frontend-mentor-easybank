import React from 'react';
import { Header, Banner, Layout, Services } from '../components';

const Home: React.FC = () => (
  <Layout>
    <Header />
    <Banner />
    <Services />
  </Layout>
);

export default Home;
